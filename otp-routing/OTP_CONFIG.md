# OpenTripPlanner Configuration Guide

This guide has moved to `docs/otp-routing/OTP_CONFIG.md` so the documentation lives under `docs/`.

Use `otp-routing/router-config.json` as the source file and let the launcher copy it into `otp-routing/assets/router-config.json` at startup.
